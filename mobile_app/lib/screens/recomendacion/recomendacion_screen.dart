import 'package:flutter/material.dart';

class RecomendacionScreen extends StatelessWidget {
  const RecomendacionScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Recomendacion Screen'),
    );
  }
}