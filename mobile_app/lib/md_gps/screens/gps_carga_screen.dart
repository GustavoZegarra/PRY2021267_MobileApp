import 'package:flutter/material.dart';

class GpsCargaScreen extends StatelessWidget {
  const GpsCargaScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}